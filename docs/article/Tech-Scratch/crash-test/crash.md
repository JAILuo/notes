通过SD卡拷贝对应板卡上的文件（因为板卡目前没有网络功能，我手上也没有网线）

| **01** | 硬件识别 | #fdisk -l /dev/mmcblk1                                       | 检查内核是否识别到物理分区   |
| ------ | -------- | ------------------------------------------------------------ | ---------------------------- |
| **02** | 挂载设备 | #mkdir -p /mnt/sdcard && mount -t vfat /dev/mmcblk1p1 /mnt/sdcard | 建立文件系统映射关系         |
| **03** | 执行传输 | #cp /boot/*.dtb /mnt/sdcard/ && sync                         | 将板子上的设备树文件拷入SD卡 |
| **04** | 安全卸载 | #sync && umount /mnt/sdcard                                  | 防止数据丢失，物理拔出       |







# introduction

最近在看到一些师弟买了一些八股文/面经资料回来看/背，回想自己去年秋招的时候，可以说几乎没怎么背过这些问题（遇到问题现场想，现场推，面试官说答错了我就直接问），我不评价八股文如何如何，但我就是很厌恶这个东西。

我一直认为的是：合格的面试官问合格的问题是考察你对 `xxx` 部分了解的多少。但为啥现在似乎变了呢？哦对，国内的环境，你只要人过来干活就行啦，想这么多干嘛！工作而已嘛，早点被 AI 消灭吧！

扯多了，本文的目的是想验证一个自己之前玩的问题：

“为什么 `spinlcok` 后不能 `sleep`？为什么中断处理中不能实现 `sleep`？......类似的问题”，

同时梳理部分几年前自己没有写下的相关基础内容（进程、并发）。





我觉得还缺少一些背景知识，突然！！我觉得我应该按照自己的想法。

下面是我的想法设计。

我知道了并发。

先不要说什么 `process`.



## 1.1 program → process（程序 → 进程）

最基础的：

> **While a computer program is a passive collection of instructions typically stored in a file on disk, a process is the execution of those instructions after being loaded from the disk into memory^[1]^**

> **计算机程序是通常存储在磁盘文件中的一组被动指令集合，而进程则是这些指令从磁盘加载到内存后执行的过程。**

就像下面这个图：

```C
                +---------------------------+ 
			    |         PROGRAM-01         |
                +---------------------------+  
			    |         PROGRAM-02         |
                +---------------------------+  
			    |         PROGRAM-03         |
                +---------------------------+
 			    |         PROGRAM-04         |
                +---------------------------+
			    |         ..........         |
			    |         ..........         |
                +===========================+  
                
```

作为一种 `conceptual model`，程序都是按照顺序规则（`sequential`）放在提前规划好的内存地址空间中属于他们的部分。

只不过现代的 `process`（或者说本来 `process` 被发明出来就是这样，六七十年代应该就有MMU了^[2]^）依赖于MMU给程序执行起来时带了一个 VR 眼镜，认为 SoC（或者说 `cpu + bus`）所提供的整个内存地址空间都是每一个执行实体的（也就是 `Virtualization` 了^[3]^ ）。

还有一个有意思的例子是：进程是动起来的程序。

> **"A process is fundamentally a container that holds all the information needed to run a program... A process is essentially an executing program, including the current values of the program counter, registers, and variables."^[4]^**

> **进程本质上是一个容器，用于存储运行程序所需的所有信息……进程本质上是正在执行的程序，包括程序计数器、寄存器和变量的当前值**



## 1.2 multitasking（多任务）

之后便到了大家常说的多任务处理（`multitasking`）。

每个 `cpu core` 一次只能执行一个进程，但依赖于 OS 提供的虚拟化能力，每个 `cpu core` 能够在各个 `process` 中来回切换，无需等待每个进程全部执行完毕（所谓的抢占式调度）。而依赖于 `cpu core`飞快的执行速度，我们所感知到的就变成有多个 `cpu core` 在帮我们做事。

> 不像我们人这种只能单线做完一件事情再做下一件事的生物。当然，具体到 `ms/us/ns...` 这种可以说某一时刻的词语，`cpu core`，自然也是只能做一件事情，但如果是 `multi-core`，那天生就是并行，自然实现真正多开、真正多任务，也就像多人做同一个任务。

这种技术，也叫 `time-sharing`：多个任务或用户同时共享计算资源。

类似的，我们常常也会听到要实现多任务/多进程的目标，其底层实现手段往往会依赖 `time-sharing`。

更进一步讨论多任务，如果从任务切换的角度来看，基本可以分为：

- `cooperative multitasking / non-preemptive multitasking`（协同多任务）
- `preemptive multitasking` （抢占多任务）

> 如果从系统设计的角度来分类，往往大家还会说有 RTOS、分时 OS、通用 OS...就像教科书上写的那样，挺混乱的。

对于协同多任务，它的工作方式基于一个约定：用户进程周期性地主动让出 `CPU` 的控制权，从而让其它进程得到运行的机会，而这件事往往需要操作系统提供一个特殊的系统调用：`sys_yield`^[5]^

> e.g. `Linux` 中的 `sched_yield`（系统调用）、 `FreeRTOS` 中的 `taskYIELD`。

但协同多任务不是本文的重点，重点是抢占多任务：`Preemptive`。

简单来说，抢占多任务是基于硬件中断（通常是时钟中断）强行进行上下文切换的：该机制会暂停当前正在执行的进程，然后由调度程序来决定下一个应执行的进程，这样一来，所有进程在任何时候都能获得一定的 CPU 使用时间。

> **对处理器上正在执行的任务进行此类操作，被称为上下文切换。**



## 1.3 preemption（抢占）

### 1.3.1 mental model（心智模型）

按照我自己的理解，`preemptive multitasking` 是用来区分：

*那种允许任务被抢占的系统* 与 *那种需要进程或任务在不需要系统资源时主动让出资源的协作式多任务系统*。

也就是 **抢占** 这个动作，`preemption` 这个词意味着：**在别人之前拿过来**。

> - **Pre-**（前缀）：表示“之前”、“在前面”。
> - **-empt-**（词根）：源自拉丁语 *emere*，意为“拿取”、“购买”。
> - **-ion**（名词后缀）：表示行为或状态。

在计算机领域，这个抢占我们一般说的是 `CPU` 控制权的转移，而且是非自愿的转移，别人强行操控；更上层软件一点的就是：当前执行的程序被迫停下来，另外一个程序抢占执行。

综上，我自己总结了一个“状态机转移”来描述上述这个任务（没错，又是这篇写的：[backtrace（一）：从底层到本质：理解函数调用与 LR/FP 的设计与思想](https://mp.weixin.qq.com/s/ULNN7Pz7TmwUwRRB9U1HGg)）：

**触发器（trigger） -> 仲裁器（arbiter） -> 执行（action）**

触发器，很好理解，因为任务不会自动交出执行权，要实现 `preemption` 那就要外部强制介入了，那就是硬件中断来强行剥夺当前执行流。具体用两种：

- **定时器中断：** 系统的心跳，Tick（很常见了，MCU里的 `systick` 记得吧？类似的）

    定时器中断定期强制打断当前任务，把控制权交还给内核。

- **外部设备中断或 IPI（核间中断）：** 网卡收到数据包、`disk` 读取完成等。

    设备会打断当前任务，唤醒等待这些数据的任务。这是实现“基于事件/优先级抢占”的物理基础。

仲裁器，自然就是决定当前任务被抢占后下一步执行什么啦，也就是内核的调度器做的事情了。中断把控制权交给了内核调度器，调度自然是有它的判断策略来选择下一步该做什么，这也就是调度策略了。

最后是执行，如果仲裁器决定需要切换当前执行流了，且是安全的（比如后面会说的：`preempt_count == 0`），那内核就会做上下文切换了（保存当前任务的寄存器状态，加载新任务的寄存器状态，完成抢占）。



### 1.3.2 调度策略

这里可以具体看看 1.3.1 所说的仲裁器所选择的调度策略。

记住首要目标，调度器是要 `preemption`（抢占），从 `first principle` 看，要看一个事情/程序能不能执行，按照人的思想，无非就是：

事情是否紧急？是否有时间做？若紧急肯定紧急的先，不紧急那就公平来做。

所以核心就两个点：**紧急、公平**

也就分别对应了目前实现主流调度策略的基础：时间片（抢占）、优先级/事件（抢占）。

- 时间片：众生平等，大家都一样

    给每一个在系统中的任务分配好各自的配额（比如1ms），每个任务的配额用完了，自然就会被别的任务抢占执行了。

    触发条件：定时器中断到来，内核检查当前运行任务的配额，用完了就换。

- 优先级/事件：有更紧急的且已经准备的任务（比如着火逃命肯定比你吃饭重要）

    别人的优先级高，自然要等别人做完才行。低优先级任务先做着，更高优先级的来了自然轮到它跑。

    触发条件：外部中断唤醒了一个高优先级任务、当前任务释放了锁，导致高优先级任务就绪。

> 单纯实现时间片以及优先级，其实在现代大型的系统已经比较少了。
>
> 对于时间片，更多时候是平时自己玩、教程之类的会选择，因为这部分讲起来，理解比较容易，就像 NJU 的 PA^[5]^的默认实现（这个 lab 可以说是计算机系统基础国内顶尖的了，涵盖全面，你要是自己改造调度也是可以的）。
>
> 对于优先级，这部分可以说是现代 RTOS 的核心调度方式了（`FreeRTOS`、`QNX`、`Zephyr`好像也是？）、Linux也有（`SCHED_FIFO` 和 `SCHED_RR` 实时调度类）

但是，内核的调度或者说实际实现的调度器没有上面说的这么简单了，最开始就说了调度关键就紧急和公平。这两个可以说“道”，实际内核会综合起来玩各种各样的花活。

> **AI 总结看看（不保证下面的正确性，请自行验证，只是拓宽视野）：**

- CFS：用“时间”的尺度来衡量“优先级”

    在传统的调度器里，时间片和优先级是两套分离的系统（高优先级分的时间片长，低优先级分的时间片短）。

    CFS（完全公平调度器）的核心思想是：**彻底消灭传统意义上的“时间片”和“绝对优先级”，把优先级转换成“时间的流逝速度”。**

    在 CFS 中，优先级（Nice 值）被映射为任务的权重（Weight）。

    任务运行的虚拟时间 $\Delta vruntime$ 与实际物理时间 $\Delta exec\_time$ 的关系如下公式表示：

    
    $$
    $$\Delta vruntime = \Delta exec\_time \times \frac{weight_{NICE\_0}}{weight_{task}}
    $$
    

    - 如果你的**优先级高**（权重极大），分母变大，你的 $vruntime$ 增长得就极慢。
    - 调度器每次都挑 $vruntime$ 最小的执行。因为你的 $vruntime$ 涨得慢，所以你就会**变相获得更多的物理执行时间**（抢占别人）。

    **结论：** CFS 看起来是“比进度（`vruntime`）”，但底座依然是**时间轮转**，而它的驱动力引擎依然是**优先级**。

- EEVDF：用“时间”动态计算“优先级”

    如果说 CFS 是“优先级决定时间分配”，那么 Deadline 和较新的 EEVDF（最早合格虚拟截止时间优先）算法，就是反过来的：**“时间决定了当下的优先级”。**

    在绝对优先级模型（如 `SCHED_FIFO`）中，优先级是一个静态的数字（比如 99）。但在 Deadline 模型中：

    - 任务的优先级变成了动态的：**距离 Deadline 越近，隐式优先级就越高。**
    - 当一个低优先级任务快要错过死线时，它的“动态优先级”会瞬间飙升，从而抢占当前的运行任务。

    **结论：** 这依然没有跳出这两大支柱，它只是一种**基于时间戳动态生成优先级**的高级抢占策略。





### 1.3.3 总结

> **说了这么多，其实是给几年前的自己学习的时候一个回答吧（虽然自己早就知道了，但之前没有记录下来的习惯）。**

在我刚学 `FreeRTOS` 的时候，其实是不能理解时间片这个东西的，说是吧时间切成一片片的，但是没用过+没人指点/没找到好的资料/脑子笨，我一直觉得有点不太通顺。加之当时身边人都是认为就写写应用代码，不用了解。

但直到我完成 NJU PA^[5]^ 后，实现了定时器中断+时间片，也才明白只要把程序运行和时间片结合起来理解就行了。

更加重要的是，当我第一次接触 Linux，我时不时就会结合 `FreeRTOS` 的优先级抢占的机制去理解，自然也就会想到实时 Linux 的内容，加上由于时不时就能在微信公众号/网络博客上搜集到各种N手/质量参差不齐的资料，

导致我第一次了解 Linux 的`preemption`（抢占）的时候，**我几乎默认这实时 Linux 是它的优先级抢占实现的，就简单的认为 Linux 已经做到了实时。把抢占和 `time-sharing`、`Priority `**。

当然现在经过实践之后的理解，也纠正了一些概念了，算是进一步反向理解 `cooperative multitasking` 了吧哈哈。



> ⚠️⚠️⚠️**本文并不会引入过多深入的实时系统的内容，我的文章大致也只包括：**
>
> ```C
> Process
> → Scheduler
> → Context Switch
> → Sleeping
> → Wakeup
> → Spinlock
> ```
>
> 不过 Linux 的实时补丁（`PREEMPT_RT`）（好像 Linux 6.12 已经合入主线了，现在也有很多文章写）
>
> 感兴趣的话直接到本文的最后（参考的前面）或者评论区，去看一些文章吧！





## 1.4 并发

要回答我最初的疑问，单凭上面的基础还是不够的。现在补充并发/ `spinlock` 的内容。

首先 `spinlock` 是用来干啥的？



> 关闭抢占和关中断一样吗？







# TODO

最后，给出上面的内容对 我在OEM 开发的一些建议指导。（驱动、OTA、功耗、性能等等）



# TMP

> 你的直觉其实是对的，而且你已经碰到了一个很多教材都会混淆的问题：
>
> > **Preemption（抢占）**
> >
> > 和
> >
> > **Time-sharing（分时）**
> >
> > 不是同一个概念。
>
> 很多书讲 Linux 调度的时候，会把两者揉在一起讲，结果初学者就会以为：
>
> > 抢占 = 时间片轮转 = 多任务
>
> 实际上从 First Principle 来拆开看，它们解决的是三个完全不同的问题。
>
> ---
>
> # 第一层：为什么会有 Process？
>
> 最底层的问题其实是：
>
> > 一台计算机如何同时服务多个工作？
>
> 假设只有一个 CPU。
>
> 它某个时刻只能执行一条指令。
>
> 因此：
>
> ```text
> CPU
>  |
>  +--> Task A
>  |
>  +--> Task B
>  |
>  +--> Task C
> ```
>
> 不可能真的同时执行。
>
> 于是 OS 必须提供一种幻觉：
>
> ```text
> Task A:
> 我独占CPU
> 
> Task B:
> 我独占CPU
> 
> Task C:
> 我独占CPU
> ```
>
> 这就是 Process + Virtual Memory 的来源。
>
> 所以：
>
> ```text
> Virtual Memory
> 解决：
> 谁拥有内存？
> 
> Process
> 解决：
> 谁拥有CPU？
> ```
>
> ---
>
> # 第二层：如何让多个 Process 共存？
>
> 这里出现了两种路线：
>
> ## Cooperative Multitasking
>
> 例如：
>
> ```c
> while (1) {
> 
>     do_something();
> 
>     yield();
> }
> ```
>
> 进程自己说：
>
> ```text
> 我干完了
> 轮到别人了
> ```
>
> CPU 从不强制打断。
>
> Mac OS Classic
>
> Windows 3.x
>
> 早期 GUI 系统
>
> 大量 RTOS
>
> 都玩过这一套。
>
> ---
>
> 它的本质是：
>
> ```text
> 切换权
> 属于任务自己
> ```
>
> ---
>
> 问题来了。
>
> 假设：
>
> ```c
> while (1) {
> }
> ```
>
> 死循环。
>
> 永远不 yield。
>
> 整个系统死掉。
>
> ---
>
> 于是出现了：
>
> # Preemptive Multitasking
>
> 核心思想：
>
> ```text
> 切换权
> 属于OS
> 而不是任务
> ```
>
> 即：
>
> ```text
> Task:
> 我不想停
> 
> OS:
> 不好意思
> 必须停
> ```
>
> 这才叫：
>
> ```text
> Preemption
> ```
>
> 抢占。
>
> ---
>
> 注意：
>
> 这里还没有时间片的概念。
>
> 仅仅是：
>
> ```text
> OS有能力
> 强制拿走CPU
> ```
>
> 就已经叫 Preemption 了。
>
> ---
>
> # 第三层：为什么会有 Time-sharing？
>
> 这是另一个问题。
>
> 假设已经支持抢占。
>
> 现在有：
>
> ```text
> Task A
> Task B
> Task C
> ```
>
> OS 可以随时打断它们。
>
> 但：
>
> ```text
> 什么时候打断？
> ```
>
> 这是新的问题。
>
> ---
>
> 最简单方案：
>
> ```text
> 每10ms
> 切一次
> ```
>
> 于是：
>
> ```text
> A -> B -> C -> A -> B -> C
> ```
>
> 形成：
>
> ```text
> Time Slice
> ```
>
> 时间片。
>
> ---
>
> 这时候才出现：
>
> ```text
> Time-sharing
> ```
>
> 即：
>
> ```text
> CPU时间
> 被切成小块
> 轮流分配
> ```
>
> 所以：
>
> ```text
> Preemption
> 是能力
> 
> Time-sharing
> 是策略
> ```
>
> 这是最重要的区分。
>
> ---
>
> 可以画成：
>
> ```text
>                Scheduler
> 
>                     |
>                     |
>           +---------+---------+
>           |                   |
>           |                   |
>      Preemption         Time-sharing
> 
>       能否抢占          如何分CPU时间
> ```
>
> ---
>
> # 第四层：你发现的问题在哪里？
>
> 你写道：
>
> > preemptive multitasking 这个词其实有点不妥
>
> 其实问题不在词。
>
> 而在于今天大家使用它时的语境变了。
>
> ---
>
> 1960年代：
>
> 抢占式多任务意味着：
>
> ```text
> 任务不需要主动yield
> 
> OS可以打断
> ```
>
> 这是革命性的。
>
> 因为当时大家主要从：
>
> ```text
> 协同多任务
> ```
>
> 迁移过来。
>
> ---
>
> 所以：
>
> ```text
> Preemptive Multitasking
> ```
>
> 强调的是：
>
> ```text
> 谁拥有CPU控制权
> ```
>
> 而不是：
>
> ```text
> 时间片
> ```
>
> ---
>
> 今天你已经默认：
>
> ```text
> OS当然能抢占
> ```
>
> 于是再看到：
>
> ```text
> Preemptive
> ```
>
> 你自然会想：
>
> ```text
> 谁抢谁？
> ```
>
> 然后联想到：
>
> ```text
> 高优先级任务
> 抢占低优先级任务
> ```
>
> 这其实是更现代的理解。
>
> ---
>
> # 第五层：真正的 Priority Preemption
>
> 你觉得：
>
> > preemption 应该更像高优先级抢占低优先级
>
> 实际上这叫：
>
> ```text
> Priority-based Preemption
> ```
>
> 例如：
>
> ```text
> Task A  priority=100
> 
> Task B  priority=10
> ```
>
> 此时：
>
> ```text
> A Ready
> ```
>
> 立刻：
>
> ```text
> B 被打断
> 
> CPU -> A
> ```
>
> 这种才是你脑海中的：
>
> ```text
> 抢占
> ```
>
> ---
>
> Linux 完全支持这一点。
>
> 例如：
>
> ```text
> SCHED_FIFO
> SCHED_RR
> ```
>
> 实时调度类。
>
> ---
>
> # 第六层：Time-sharing 与 Priority Preemption 是正交的
>
> 很多人混淆这里。
>
> 实际上：
>
> ```text
> Time-sharing
> ```
>
> 和
>
> ```text
> Priority
> ```
>
> 是两个维度。
>
> ---
>
> 例如 Linux CFS：
>
> ```text
> 同优先级任务
> 
> 轮流获得时间片
> ```
>
> 属于：
>
> ```text
> Time-sharing
> ```
>
> ---
>
> RTOS：
>
> ```text
> 最高优先级任务
> 一直执行
> 
> 直到阻塞
> ```
>
> 属于：
>
> ```text
> Priority Preemption
> ```
>
> ---
>
> 两者可以同时存在。
>
> 例如：
>
> ```text
> Priority Level 100
>     A
>     B
>     C
> ```
>
> 先选最高优先级：
>
> ```text
> 100
> ```
>
> 再在内部：
>
> ```text
> A B C
> 轮转
> ```
>
> 这就是：
>
> ```text
> Priority + Time-sharing
> ```
>
> ---
>
> # 第七层：RTOS 和 Linux 的本质区别
>
> 这里是很多人第一次接触实时系统时最容易误解的。
>
> 很多人会说：
>
> ```text
> Linux支持抢占
> 
> RTOS也支持抢占
> 
> 有什么区别？
> ```
>
> 其实区别根本不在抢占。
>
> ---
>
> 从 First Principle：
>
> RTOS 追求：
>
> ```text
> 确定性（Determinism）
> ```
>
> 而不是：
>
> ```text
> 公平性（Fairness）
> ```
>
> ---
>
> Linux 追求：
>
> ```text
> Everybody gets CPU
> ```
>
> 大家都公平。
>
> ---
>
> RTOS 追求：
>
> ```text
> Highest priority wins
> ```
>
> 最高优先级永远赢。
>
> ---
>
> 例如：
>
> ```text
> 电机控制任务
> 
> 优先级100
> ```
>
> 来了就跑。
>
> ---
>
> 浏览器：
>
> ```text
> 优先级5
> ```
>
> 饿死都无所谓。
>
> ---
>
> 因此 RTOS 常见行为：
>
> ```text
> Task A priority=100
> 
> while (1)
> {
> }
> ```
>
> 整个系统都可能只跑它。
>
> 因为这是设计目标。
>
> ---
>
> # 第八层：PREEMPT_RT 到底解决什么？
>
> 这里又是一个常见误区。
>
> 很多人以为：
>
> ```text
> PREEMPT_RT
> 
> =
> Linux支持抢占
> ```
>
> 错。
>
> Linux 2.6 时代就支持抢占了。
>
> ---
>
> PREEMPT_RT 解决的是：
>
> ```text
> 抢占延迟
> ```
>
> （Preemption Latency）
>
> ---
>
> 即：
>
> ```text
> 高优先级任务准备运行
> 
> 到真正拿到CPU
> 
> 需要多久？
> ```
>
> ---
>
> 普通 Linux：
>
> ```text
> 几十微秒
> 几百微秒
> 几毫秒
> ```
>
> 都可能。
>
> ---
>
> PREEMPT_RT：
>
> 目标是：
>
> ```text
> 可预测
> ```
>
> 例如：
>
> ```text
> < 50 us
> ```
>
> ---
>
> 注意：
>
> 这里仍然是：
>
> ```text
> Determinism
> ```
>
> 而不是：
>
> ```text
> Throughput
> ```
>
> ---
>
> # 我建议你文章里的表述
>
> 你真正想表达的，其实可以浓缩成一句：
>
> > Preemption（抢占）描述的是“操作系统是否有权强制夺回 CPU”；Time-sharing（分时）描述的是“操作系统如何分配 CPU 时间”；Priority Scheduling（优先级调度）描述的是“多个可运行任务之间谁应当优先获得 CPU”。三者经常同时出现，但解决的是不同层面的问题，因此不应混为一谈。
>
> 如果你后面准备引出 spinlock、msleep、调度器甚至 PREEMPT_RT，我反而建议不要在这里展开实时系统。
>
> 因为对于你的文章主线：
>
> ```text
> Process
> → Scheduler
> → Context Switch
> → Sleeping
> → Wakeup
> → Spinlock
> ```
>
> 最关键的是先把：
>
> ```text
> Preemption
> ≠ Time-sharing
> ≠ Priority Scheduling
> ```
>
> 这个认知钉牢。
>
> 后面讲 Linux 的 `schedule()`、`need_resched`、`preempt_count`、`spin_lock()` 为什么要关闭抢占时，你会发现整个逻辑链会非常顺。
>
> 对于spinlock的部分，假如说是在FreeRTOS中的，有这么个组件，是不是可以说就是进入临界区，简单类比就行。
> 在这个部分，如果是单核裸机上，进入临界区只要关了中断，这部分的内容就独占保护；如果是RTOS，关了中断也是OK的。
> 因为是单核，只要保证了原子性就行：
> 一条指令就可以实现原子性
> x86: cli 清除 eflags 中的 IF bit
> RISC-V: 清除 mstatus 的 MIE bit
> AArch64: msr daifset, #3
> 程序死循环 = 计算机系统卡死
>
> 但是如果是在多核上，不太行：
> 单处理器系统可以，多处理器系统不行
> 每个处理器有独立的寄存器组
> 中断是每个处理器内部状态











不过，多任务处理允许处理器在各个进程之间切换，而无需等待每个进程全部执行完毕（即所谓的“抢占式调度”）



反正每一次调度，OS自己都会选择一个执行实体来去执行（选哪个执行实体，就靠OS内核自己的调度器了CFS、EEVDF...）







既然说是线程调度。调度的是进程还是线程？？

需要梳理清楚，清楚之后，然后进一步去去看看现代的Linux内核是怎么调度的。

为什么会看到preempt？





# 参考

[1] Process (computing) - Wikipedia：https://en.wikipedia.org/wiki/Process_(computing)

[2] MMU的OS

[3] OSTEP

[4] — Andrew S. Tanenbaum, Herbert Bos. *Modern Operating Systems*, 5th Edition (2023), P39, P86

[5] 来自外部的声音 · GitBook：https://nju-projectn.github.io/ics-pa-gitbook/ics2025/4.4.html





# 文章

> 你的直觉很准，这确实是个容易混淆的概念，需要从**术语定义**和**Linux内核历史**两个维度来看。
>
> ## 1. 为什么“时间片到期强制切换”也是抢占？
>
> 你理解的 **preemption = 先于他人夺取某物** 是对的，这正是操作系统中的定义：
>
> > **抢占（Preemption）** ：操作系统**无需等待当前任务主动让出**，就可以强制暂停它，把CPU分配给另一个任务。
>
> 关键点在于：**强制性的来源并不限于是“高优先级任务来临”，也可以是“时间片用完”**。这两种都是典型的抢占触发条件：
> - **基于优先级**：高优先级任务就绪 → 抢占低优先级任务。
> - **基于时间片**：时间片耗尽 → 操作系统强制切换（哪怕任务不想让出）。
>
> 你前面说的 **time‑sharing（时间共享）** 是一种**调度策略**，它通过**时间片轮转**来实现公平使用CPU。而时间片到期后切换任务，靠的正是**抢占机制**——因为系统没有等那个进程调用 `sched_yield`，而是直接把它“拽”下来。
>
> 所以这两者并不冲突，而是：
> ```
> 抢占式多任务（Preemptive Multitasking）
>     └── 实现策略之一：时间共享（Time‑Sharing）
> ```
> 现代Linux既是抢占式多任务系统，又采用CFS（完全公平调度器）这种本质上属于时间共享思想的调度策略。
>
> ## 2. 那为什么早期Linux（无实时补丁）就有 `preempt_xxx`？
>
> 这就是内核历史演进的缘故。Linux的“抢占”是分阶段逐步完善的：
>
> ### 阶段一：用户态抢占（早期Linux 1.x/2.4）
> - 用户进程在用户态运行时，**可以被抢占**（定时器中断或其他中断唤醒更高优先级进程后，强制调度）。
> - 但一旦进程进入**内核态**（比如系统调用），这个内核代码就会一口气跑到完、或者跑到主动阻塞为止，**期间无法被抢占**。
> - 这意味着：如果一个低优先级进程正在内核里拷贝大量数据，实时任务必须等它干完。这就是**内核不可抢占**的局限，延迟可能达到几十毫秒。
>
> ### 阶段二：内核抢占引入（Linux 2.6）
> 为了改善交互和降低延迟，2.6内核引入了**内核可抢占（Kernel Preemption）**特性，也就是允许内核代码在执行时，在安全点上被抢占。
>
> 这些安全点是怎么管理的？就是通过你现在看到的那些 `preempt_disable()` / `preempt_enable()` 等函数。
> - 每个任务的 `thread_info` 里有一个 `preempt_count` 计数器。
> - 当内核需要保护一段不能被打断的临界区时，会调用 `preempt_disable()` 增加计数。
> - 中断返回或特定时机下，调度器检查 `preempt_count`：如果为零，说明当前可以安全抢占，就会触发调度。
> - 这些函数**在没有任何实时补丁的标准内核里就已经存在**，它们是实现内核抢占的基础机制。
>
> **所以，即使没有实时补丁，Linux也已经是一个“支持内核抢占”的抢占式系统**，只不过它的抢占点还受限于自旋锁等临界区。
>
> ### 阶段三：完全抢占（PREEMPT_RT 补丁）
> 实时补丁做了什么呢？它没有重新发明“抢占”，而是把内核里**无法抢占的死角彻底打开**：
> - 把自旋锁替换为可睡眠的互斥锁，使得锁保护的临界区也可被抢占；
> - 把中断处理程序线程化，让中断处理也服从调度；
> - 最终达到“除了极少数底层操作，内核任何位置都可被抢占”。
>
> 所以你可以把这三个阶段理解成“抢占的深度递增”：
>
> |            | 用户态 | 内核态（非临界区） | 内核态（自旋锁等临界区） | 中断上下文             |
> | ---------- | ------ | ------------------ | ------------------------ | ---------------------- |
> | 早期Linux  | 可抢占 | 不可抢占           | 不可抢占                 | 可抢占（中断总会抢占） |
> | Linux 2.6  | 可抢占 | **可抢占**         | 不可抢占                 | 可抢占                 |
> | PREEMPT_RT | 可抢占 | 可抢占             | **基本可抢占**           | **线程化，可调度**     |
>
> ## 3. 小结：为什么你看到了很多 `preempt_xxx`？
>
> **因为这些函数是实现“内核抢占”的开关**，早在2.6内核时代就已经存在。它们并不是实时补丁专有的，而是Linux逐步演化成“更彻底抢占式系统”的历史足迹。
>
> 用一句话回应你的疑问：
>
> > 之前的Linux内核确实已经有基于时间片的抢占（time‑sharing），同时为了低延迟又逐步引入了**内核内部抢占点**的控制机制，这些 `preempt_xxx` 就是用来管理那些“现在可以/不可以被抢占”的开关。实时补丁只是把这个开关几乎全部拨到了“可以”的位置。
>
> 如果你想看看这些函数具体在代码里怎么用，我可以挑一个经典的锁操作路径，分析一下 `preempt_disable` 是如何保护临界区的。





注意 DWARF 的 CFI 信息，内核的这些就用的还是 `debug_frame`，但如果是用户态的，那一般都是 `eh_frame` 了。

```BASH
 aarch64-linux-gnu-readelf -s crash_test.ko | grep frame
    24: 0000000000000000     0 SECTION LOCAL  DEFAULT   31 .debug_frame

```





### 1. “多任务”就是“多进程”吗？

**不完全相同，但密切相关。**

“多任务”（Multitasking）是指**操作系统同时管理多个执行流的能力**，而“进程”（Process）是实现这种能力的一种载体。

- **多任务可以基于进程实现**：早期 Unix、Windows 等系统中，多任务就是让多个进程轮流使用 CPU，每个进程有独立的地址空间和资源。
- **多任务也可以基于线程实现**：一个进程内部可以有多个线程，它们共享进程资源，但各自独立调度，这也是一种多任务（准确说是多线程并发）。比如你打开一个浏览器，里面多个标签页可能各自由不同线程渲染，这就是“多任务”在同一个进程内的体现。
- **更轻量的协程、纤程等也可以构成多任务**（用户态调度）。

所以严格来说：
多任务 ≠ 多进程，但 **多进程是多任务的一种重要实现方式**。
现代操作系统中，多任务通常同时指进程级并发和线程级并发，调度器甚至只调度线程（如 Linux 中调度的是线程/任务，而非整个进程）。



- **分时系统** — 追求交互公平（你文中的 time‑sharing）
- **实时系统** — 追求时间确定性
- **批处理系统** — 追求吞吐量，不要求交互

